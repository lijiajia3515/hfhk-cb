apply from: "${rootDir}/gradle/publish.gradle"

dependencies {
  compile "com.hfhk.cairo:cairo-domain:${cairoVersion}"
  compile "com.hfhk.auth:auth-domain:${authVersion}"
  compile "com.hfhk.system:system-domain:${systemVersion}"
}
